<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GalleryZIP" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:windowBackground">@color/background_night</item>
        <item name="android:buttonStyle">@style/ButtonsStyle</item>

    </style>

    <style name="ButtonsStyle">
        <item name="android:textColor">@color/buttons_text_night</item>
        <item name="backgroundTint">@color/buttons_tint_night</item>
        <item name="android:padding">10sp</item>
    </style>

    <style name="FolderVectorImage">
        <item name="tint">@color/white</item>
    </style>

    <style name="BackButton">
        <item name="tint">@color/white</item>
    </style>


</resources>